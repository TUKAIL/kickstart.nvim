# Neovim 写 C 的基础使用说明

这份配置已经帮你加好了 C 开发最基础的一套能力：

- `clangd`：代码提示、跳转定义、引用、报错提示
- `clang-format`：代码格式化
- `treesitter`：C 语法高亮
- 编译 / 运行快捷键

---

## 1. 你现在已经有的功能

打开 `*.c` 或 `*.h` 文件后，一般会有：

- 补全提示
- 语法报错/警告
- 跳转定义
- 查看引用
- 保存时自动格式化
- 手动格式化
- 快捷编译、快捷运行

---

## 2. 首次使用建议

进入 Neovim 之后，先执行：

```vim
:Mason
```

检查下面两个工具是否已经安装成功：

- `clangd`
- `clang-format`

如果没有装好，就在 `:Mason` 里面手动安装。

---

## 3. 常用快捷键

### LSP / 提示相关

这些是最常用的：

- `grd`：跳转到定义
- `grr`：查看引用
- `grD`：跳转到声明
- `gri`：跳转到实现
- `grt`：跳转到类型定义
- `grn`：重命名符号
- `gra`：代码操作（修复建议等）
- `<leader>q`：打开诊断列表

说明：

- `<leader>` 在这份配置里是空格键，所以像 `<leader>q` 就是 `空格 + q`

### 查找相关

- `<leader>sf`：找文件
- `<leader>sg`：全文搜索
- `<leader>sd`：搜索诊断信息
- `<leader>sh`：搜索帮助文档

### 格式化

- `<leader>f`：手动格式化当前文件

### C 编译/运行

这次额外加了两个快捷键：

- `<leader>cc`：编译当前 C 文件
- `<leader>cr`：运行当前编译出的程序

也就是：

- `空格 cc`：编译
- `空格 cr`：运行

编译命令当前使用的是：

```bash
gcc -Wall -Wextra -std=c11 当前文件 -o 当前文件名(去掉.c)
```

例如你当前编辑的是 `main.c`，那么编译后会得到：

```bash
./main
```

运行快捷键就是执行这个程序。

---

## 4. 一个最简单的使用流程

例如你写一个 `main.c`：

```c
#include <stdio.h>

int main(void) {
    printf("hello\n");
    return 0;
}
```

然后：

1. `空格 cc` 编译
2. `空格 cr` 运行

如果只是做基础练习，这样已经够用了。

---

## 5. 自动格式化说明

目前已经为下面文件类型开启保存自动格式化：

- `c`
- `cpp`

也就是说你保存时会自动调用 `clang-format`。

如果不想等保存，也可以直接按：

- `空格 f`

手动格式化。

---

## 6. 很重要：为什么有时候提示不准

C 的提示是否准确，很依赖编译参数。

例如这些信息会影响 `clangd`：

- 头文件搜索路径
- 宏定义
- 使用的 C 标准（如 `c11` / `c17`）
- 工程编译选项

如果你只是写单文件小练习，通常问题不大。

但是如果你写的是一个稍微正式一点的项目，最好给 `clangd` 提供编译信息。

---

## 7. 正式项目推荐：提供 `compile_commands.json`

如果项目用的是 CMake，推荐这样生成：

```bash
cmake -S . -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
```

然后在项目根目录建立链接：

```bash
ln -sf build/compile_commands.json compile_commands.json
```

这样 `clangd` 就能更准确地知道：

- include 路径
- 宏
- 编译选项
- 实际使用的标准

这样补全、报错、跳转都会明显更准。

---

## 8. 如果只是简单单文件，也可以加 `.clangd`

如果你不是 CMake 项目，只是简单练习，也可以在项目根目录建一个 `.clangd`：

```yaml
CompileFlags:
  Add: [-Wall, -Wextra, -std=c11]
```

这能告诉 `clangd`：

- 按 `c11` 处理
- 使用一些常见警告参数

对于简单练习项目，这样已经不错了。

---

## 9. 目前这套配置的边界

这次加的是“基础可用版”，适合：

- C 入门
- 算法题
- 小程序
- 学校作业
- 小型练习项目

还没有额外加这些：

- 调试器（gdb / dap）
- `clang-tidy`
- Makefile / CMake 一键构建
- 单元测试集成

如果你后面需要，我可以继续帮你补。

---

## 10. 建议你记住的几个最核心按键

如果只记最重要的几个：

- `空格 cc`：编译
- `空格 cr`：运行
- `空格 f`：格式化
- `grd`：看定义
- `grr`：看引用
- `gra`：看修复建议
- `空格 q`：看诊断列表

---

## 11. 如果发现不工作，优先检查这些

### 1）工具有没有装好

在 Neovim 里执行：

```vim
:Mason
```

确认：

- `clangd`
- `clang-format`

都已安装。

### 2）系统里有没有 gcc

因为编译快捷键调用的是 `gcc`，所以系统里要能执行：

```bash
gcc --version
```

### 3）项目是不是缺编译参数

如果你发现：

- 某些头文件报错
- 提示不准
- 跳转不对

优先考虑是不是缺：

- `compile_commands.json`
- 或 `.clangd`

---

## 12. 后续可继续增强

如果你后面想继续完善，可以再加：

- 调试支持（gdb + nvim-dap）
- 更完整的 CMake 工作流
- `clang-tidy` 静态检查
- 任务系统（编译、运行、测试分开）

如果要继续，我建议下一步优先加“调试”。
